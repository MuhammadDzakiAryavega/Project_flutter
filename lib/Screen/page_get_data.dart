import 'package:flutter/material.dart';

class PageGetData extends StatelessWidget {
  const PageGetData({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Page Get Data"),
      ),
      body: Center(
        child: Text("Page Get Data Content Here"),
      ),
    );
  }
}
